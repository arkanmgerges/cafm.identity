apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "coral-api.fullname" . }}-test-connection"
  labels:
    {{- include "coral-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "coral-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
