apiVersion: v1
kind: Service
metadata:
  name: {{ include "coral-api.fullname" . }}
  labels:
    {{- include "coral-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPortName }}
      protocol: TCP
      name: http
  selector:
    {{- include "coral-api.selectorLabels" . | nindent 4 }}
